viewdata model="CrossFit204ScoreBoard.Web.Actions.Athletes.AthleteDetailsViewModel"

h2 =Model.Athlete.FirstName + " " + Model.Athlete.LastName
=this.LinkTo(new EditAthleteRequest{AthleteId = Model.Athlete.Id.Replace("athletes/","")}).Text("Update")

br
ul
	li each="var w in Model.Workouts"
		=w.Name
		
		